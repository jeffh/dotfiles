#import "[:EVAL:]expand('%:t:r')[:END:].h"

@interface [:EVAL:]expand('%:t:r')[:END:]
@end

@implementation [:EVAL:]expand('%:t:r')[:END:]

- (id)init
{
    self = [super init];
    if (self) {
    }
    return self;
}

@end
